# Aether — sandbox curated Python stack.
# Adding a package = rebuild the image. That's intentional (auditability).
numpy==2.0.2
pandas==2.2.3
matplotlib==3.9.2
scipy==1.14.1
sympy==1.13.3
scikit-learn==1.5.2
pillow==11.0.0
beautifulsoup4==4.12.3
lxml==5.3.0
requests==2.32.3
httpx==0.27.2
plotly==5.24.1
networkx==3.4
PyYAML==6.0.2
toml==0.10.2
ipython==8.29.0
jupyter-core==5.7.2
nbformat==5.10.4
openpyxl==3.1.5
pytest==8.3.3
black==24.10.0
ruff==0.7.4
# Office document generation — added in 0.2.18 so the model can build
# .docx / .pptx / .pdf without a runtime `pip install` (which warns about
# root + adds 1-2s per first call). These three cover ~all "make me a
# Word/PowerPoint/PDF" requests we see.
python-docx==1.1.2
python-pptx==1.0.2
reportlab==4.2.5
# SVG → PNG rendering (added v0.4.250 — was missing, model was
# falling back to `rsvg-convert` subprocess which also wasn't installed).
cairosvg==2.7.1
